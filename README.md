# `pp-prettier`

> [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @pp-prettier
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@pp-prettier"
}
```